line-length = 100
target-version = "py311"
src = ["src", "tests"]
# `scripts/` holds standalone anti-drift tooling copied from the WeKnora
# source repo; it is not application code and is not linted.
extend-exclude = ["scripts"]

[lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "DTZ", # flake8-datetimez
    "PIE", # flake8-pie
    "RET", # flake8-return
    "SIM", # flake8-simplify
    "TCH", # flake8-type-checking
    "RUF", # ruff-specific
]
ignore = [
    "E501",   # line-too-long (handled by formatter)
    "ISC001", # single-line implicit string concat (conflicts with formatter)
    # The project bans `if TYPE_CHECKING:` blocks, so ignore all TC rules.
    "TC001",
    "TC002",
    "TC003",
    "TC004",
    "TC005",
    "TC006",  # quoted type expressions - keep readable
]

[lint.isort]
known-first-party = ["src", "tests"]

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"