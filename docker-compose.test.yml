# Integration-test infra. Same Postgres + Redis as the local stack.
#
#   docker compose up -d postgres redis
#   uv run pytest tests/
#
# The historical file listed commented stubs. The running definitions
# live in compose.yaml at the repository root.

include:
  - path: compose.yaml
