# `shared-agents` endpoints

Routes registered under `/api/v1/shared-agents`. Path parameters use FastAPI `{name}` notation.

| Method | Path |
| --- | --- |
| GET | `/shared-agents` |
| POST | `/shared-agents/disabled` |
